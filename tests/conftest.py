# This file is intentionally empty.
# Its presence tells pytest, Pyre2, and Pyright that `tests/` is a package
# whose parent directory (the project root) is a valid import root.
# The root conftest.py handles sys.path setup.
